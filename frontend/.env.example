# CMDB Frontend Environment Variables
# Copy this file to .env.local and modify as needed

# API base URL (leave empty in development, Vite proxy handles /api)
# In production, set to your backend URL, e.g. https://api.example.com
VITE_API_BASE_URL=
